# Agent Notes

## Known Information
- This repository appears to be a Jekyll-based static site (due to the presence of `_config.yml`, `_posts/`, and Jekyll layout directories). Anticipated preview command: `bundle exec jekyll serve`.
- JavaScript tooling is available via `package.json` and `node_modules/`; run Node-based tooling with `npm` scripts when needed.
- Prefer fast file searches with `rg` (ripgrep) instead of recursive `ls`/`grep` for large directories.
- Follow repository-wide instructions from higher-level contexts (system/developer/user) and respect nested `AGENTS.md` files if they are added later.

## TODO / Requests for Guidance
- Confirm the canonical workflow for building and testing the site (e.g., exact commands for development and production builds).
- Provide coding style or formatting preferences for Markdown, HTML, Liquid templates, and Sass files.
- Clarify expectations for handling images/assets (naming conventions, compression requirements, etc.).
- Document any preferred commit message structure or branching strategy beyond standard Git practices.
- Specify accessibility or SEO guidelines that should be applied when editing content.
